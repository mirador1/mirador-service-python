# Build stage : multi-stage Docker image (mirror of svc docker-build)

docker-build:
  stage: package
  image: docker:28
  tags:
    - macbook-local
  needs:
    - unit-tests
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_TAG
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    # Cross-compile to amd64 via QEMU so the image matches GKE node arch
    - docker run --privileged --rm tonistiigi/binfmt --install amd64 || true
    - docker buildx create --name multi --use 2>/dev/null || docker buildx use multi
  script:
    # Multi-arch build : amd64 (GKE / EKS / cloud-run) + arm64 (M1/M2/M3
     # workstation, Raspberry Pi, Graviton). Single image manifest with
     # both arch entries — pull picks the right one automatically.
    - |
      docker buildx build \
        --platform linux/amd64,linux/arm64 \
        --tag "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHA" \
        --tag "$CI_REGISTRY_IMAGE:latest" \
        --label "org.opencontainers.image.revision=$CI_COMMIT_SHA" \
        --label "org.opencontainers.image.created=$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
        --label "org.opencontainers.image.source=$CI_PROJECT_URL" \
        --cache-from "type=registry,ref=$CI_REGISTRY_IMAGE:cache" \
        --cache-to "type=registry,ref=$CI_REGISTRY_IMAGE:cache,mode=max" \
        --push \
        .
